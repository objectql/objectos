name: user
label: User
description: Standard User Role
policies:
  - base_access

permissions:
  tasks:
    # Additive: Can also UPDATE own tasks
    actions: [update]
    filters: 
      - ['assigned_to', '=', '$user.id']
