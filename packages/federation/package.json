{
  "name": "@objectos/federation",
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0",
  "description": "Dynamic Plugin Loading for ObjectOS — Module Federation, shared dependencies, and hot-reload",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean && tsc --emitDeclarationOnly --declaration",
    "test": "jest --forceExit --passWithNoTests",
    "test:coverage": "jest --coverage --forceExit --passWithNoTests",
    "lint": "eslint src/",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@objectstack/runtime": "^3.0.6",
    "@objectstack/spec": "3.0.6"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.2.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
