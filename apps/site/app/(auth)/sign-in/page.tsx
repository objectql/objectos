import SignInForm from '@/components/auth/sign-in-form';

export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your ObjectOS account',
};

export default function SignInPage() {
  return <SignInForm />;
}
