export default {
    datasource: {
        default: {
            type: 'sqlite',
            filename: 'objectos.db'
        }
    },
    presets: [
        // Presets removed, using plugin-defined objects now
    ]
};
